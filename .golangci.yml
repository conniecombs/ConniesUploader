version: "2"

run:
  timeout: 5m

linters:
  exclusions:
    rules:
      # Ignore errcheck for Close() method calls - standard Go practice for deferred cleanup
      - text: "Error return value of `.*Close` is not checked"
        linters:
          - errcheck
